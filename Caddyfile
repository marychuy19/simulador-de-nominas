{
	frankenphp
	order php_server before file_server
	log {
		output stderr
		level INFO
	}
}

simulador-de-nominas-production.up.railway.app {
	root * /app/public
	encode zstd br gzip

	handle /health {
		respond "OK" 200
	}

	php_server
}
