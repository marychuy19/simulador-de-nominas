{
	frankenphp
	order php_server before file_server
	log {
		output stderr
		level INFO
	}
}

:80 {
	bind 0.0.0.0
	root * /app/public
	encode zstd br gzip

	handle /health {
		respond "OK" 200
	}

	php_server
}
