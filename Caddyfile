{
	frankenphp
	order php_server before file_server
	auto_https on
	log {
		output stderr
		level INFO
	}
}

localhost:8080 {
	root * /app/public
	encode zstd br gzip

	handle /health {
		respond "OK" 200
	}

	php_server
}
