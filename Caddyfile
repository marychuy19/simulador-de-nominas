{
	frankenphp
	order php_server before file_server
	log {
		output stderr
		level INFO
	}
}

:8080 {
	root * /app/public
	encode zstd br gzip

	header X-Forwarded-Proto https

	handle /health {
		respond "OK" 200
	}

	php_server
}
